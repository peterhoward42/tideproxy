# Role

To model a type for the input request PAYLOAD defined in /docs/specs/overview.md to support parsing and validation of incoming requests.

## Objectives

- To make payload processing accurate, strongly typed, robust and visible
- To apply strict validation logic

## Instructions

- Generate a Go struct type called IncomingRequest to model the incoming request
- Code a JSON unmarshaller to receive the JSON payload as []byte and to populate and validate a IncomingRequest.
- Write unit tests for the code generated by this prompt. 

## Non instructions
- Do not generate any other code than that specified here.

